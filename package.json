{
  "name": "ministry-of-truth",
  "version": "1.0.0",
  "description": "A dystopian document-redaction game.",
  "private": true,
  "scripts": {
    "build": "tsc",
    "dev": "tsc && node server.js",
    "watch": "tsc --watch",
    "start": "node server.js"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
